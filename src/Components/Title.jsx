import React from 'react'

const Title = ({name}) => {
  return (
    <h4 className='text-[20px] lg:text-[35px] font-bold text-[#282938] text-center lg:text-left'>{name}</h4>
  )
}

export default Title